#include "cube.h"

void	is_in_wall(t_vf2d point, t_map *map, int tile_size)
{
	int	y;
	int	x;

	y = 0;
	while (y < map->y_max)
	{
		x = 0;
		while (x < map->x_max)
		{
			
			++x;
		}
		++y;
	}
}

/*
fonction qui calc si <x< et <y<
set on_off le rayon et continuer de faire avancer le rayon
*/
